esses sistemas são em flask, um criar uma fila de atendimento hospitalar e o outro cria um sistema de login para paciente e para o estabelecimento no nosso sistema, quero que exista uma conexão entre usuários (estabelecimentos) logados e o sistema de inserir gente na fila. Se o paciente tiver sido cadastrado pelo estabelecimento quero que o paciente possa ver sua posição assim que logar, para ver se ele está cadastrado no estabelecimento será feito comparando as tabelas do nosso sistema (tb_cliente) e com o a tabela paciente que será criada para cada estabelecimento/empresa cadsatrada,

✅ 5. Como saber a qual empresa pertence o paciente? na tabela fila irá mostrar se o paciente está cadastrado naquele estabelecimento, cada estabelecimento tem sua própria fila e um paciente/cliente pode está em diversos estabelecimentos, a fila tem seus dados apagados sempre que o estabelecimento fizer logout, porém a tabela paciente fica tem seus dados gravados de forma persistente para analise de dados feita pelo estabelecimento de forma futura, cadastrar-paciente.html é a página html onde será feito o cadastro do paciente tanto na tabela fila quanto na tabela paciente feito pelo estabelecimento (eu já mandei ela dentro do arquivo back-end sistema de fila.zip e essa página é bem parecida, mas quero que voce adapate para mostrar ela agora). Em configuracoes-estabelecimento.html use o campo "nome da clinica" para trocar o nome da clinica dentro da tag h2 que está dentro da classe sidebar também crie um botão excluir conta que excluirá a conta do estabelecimento e apagará todos os dados referentes ele (a tabela fila e paciente referente a esse estabelecimento serão excluídas também). O arquivo lista-pacientes.html será mostrado ao estabelecimento para colocar o paciente em atendimento, removendo ele da fila (o paciente removido da fila deverá ir para o campo "Últimos Chamados" no arquivo fila.html que está dentro do back-end sistema de fila.zip ). Coloque na tabela paciente duas colunas com o tempo inicial que ela entrou na fila e o tempo final que ela saiu da fila, isso deverá ser usado para calcular o tempo médio de espera que será mostrado na tabela fila dentro do fila.html que está dentro do back-end sistema de fila.zip (essa coluna não existe dentro do table desse html, coloque ela lá). Assim que o paciente for atendido e sair da fila ele entra em uma tabela no banco de dados chamada em_atendimento, ela não existe então crie ela, ela deve ter os dados do paciente e duas colunas com o tempo de inicio de atendimento e fim do atendimento, que será mostrado no tela-principal-estabelecimento.html, essa tela deve ter um botão que remova o paciente da tabela em_atendimento, esse botão não existe no arquivo ainda então crie ele, em cada tabela do banco de dados crie uma coluna com a data que o dado foi criado (as colunas de tempo inicial, tempo final e data de criação devem ser inserida de forma defalt no sql, sem que o beck end precise fazer isso, somente criar). Crie um sistema de login para o logout (use o sair que está dentro da classe sidebar, assim que o estabelecimento deslogar ele deve retornar a página inicial), sobre o campo "profissionais" dentro da classe sidebar eu vou mexer nele depois. Construa o projeto em flask reaproveitando o que já foi feito nos arquivos .zip, use a mesma ordenação de pastas, com arquivos (models.py, db.py, routes.py, main.py, __init__,py), não altere o que já foi feito nem a lógica, só acrecente o que foi pedido